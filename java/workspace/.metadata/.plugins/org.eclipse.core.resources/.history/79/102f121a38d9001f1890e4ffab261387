package collection.map;

import java.util.*;

public class HashMapEx {
	public static void main(String[] args) {
		//<키에 대한 타입 , 값에 대한 타입>
//		HashMap<Integer, String> map = new HashMap<>();
		Map<Integer, String> map = new HashMap<>(); // 자식으로 만들어서 map에 저장
		
		//맵에 값을 추가
		map.put(1, "홍길동")
	}
}
